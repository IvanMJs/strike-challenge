export type {
  VulnerabilityState,
  VulnerabilityCriticality,
  StateOption,
  CriticalityOption
} from '../types/constants';

export {
  VULNERABILITY_STATES as STATES,
  STATE_TRANSITIONS,
  CWE_OPTIONS,
  CRITICALITY_OPTIONS
} from '../types/constants';
